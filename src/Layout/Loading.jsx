function Loading() {
  return (
    <div
      style={{
        minHeight: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <h1 style={{ color: "red", fontSize: "100px", textAlign: "center" }}>
        Loading...
      </h1>
    </div>
  );
}

export default Loading;
